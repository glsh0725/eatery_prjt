<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fs.four.eatery.user.dao.LoginDAO">

    <select id="UserLogin" parameterType="String" resultType="fs.four.eatery.user.vo.UserVO">
        <![CDATA[
        SELECT *
        FROM member
        WHERE mem_id = #{mem_id}
        ]]>
    </select>

</mapper>